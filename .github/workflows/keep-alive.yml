name: Keep Alive

on:
  schedule:
    - cron: '0 9 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl -f https://winprofit-backend-production.up.railway.app/health
          echo "Backend pinged successfully"
      
      - name: Ping Supabase
        run: |
          curl -f https://winprofit-backend-production.up.railway.app/health
          echo "Keep alive done"